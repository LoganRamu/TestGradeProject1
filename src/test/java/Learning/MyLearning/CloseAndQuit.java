package Learning.MyLearning;

//Close: close the current window
//Close: Only close not memory release, int task bar still chromedriver.exe runs
//To check: cmd->taskkill /f /im chromedriver.exe
//Quit: Close the entire browser (if multiple opens)
//Quit: Release the memory as well

public class CloseAndQuit {
}
