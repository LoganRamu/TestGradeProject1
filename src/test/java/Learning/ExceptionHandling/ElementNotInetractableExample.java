package Learning.ExceptionHandling;

public class ElementNotInetractableExample
{
    public static void main(String[] args) {
        /*Approach 1: WebDriverWait, wait.until(ExpectedConditions.visibilityOf())
        * 2: Actions actions=new Actions(driver);
        * driver.moveToElements(ele);
        * 3. JavascriptExecutor to intracte directly with DOM
        * */
    }
}
